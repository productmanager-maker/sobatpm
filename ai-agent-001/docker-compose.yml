services:
  sales-bot:
    build: .
    container_name: sales-bot
    restart: unless-stopped
    env_file: .env
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./service-account.json:/app/service-account.json:ro
